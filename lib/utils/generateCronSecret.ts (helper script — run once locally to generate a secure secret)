import { randomBytes } from "crypto";

// Run with: npx tsx lib/utils/generateCronSecret.ts
// Copy the output into CRON_SECRET in .env.local and Vercel project settings.
console.log(randomBytes(32).toString("hex"));
